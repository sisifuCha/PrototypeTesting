# Android 模拟器图片上传使用指南

## 方法一：通过模拟器扩展控制面板添加照片

1. **打开扩展控制面板**
   - 在 Android Studio 右侧找到模拟器窗口
   - 点击模拟器右侧的 `...`（更多）按钮
   - 或使用快捷键 `Ctrl + Shift + A`（Windows）/ `Cmd + Shift + A`（Mac）

2. **添加照片到虚拟相册**
   - 在扩展控制面板中选择 **"Camera"** 选项卡
   - 或选择 **"Virtual sensors"** > **"Additional sensors"**
   - 找到 **"Add image"** 或 **"Load image"** 按钮
   - 从电脑中选择图片文件（JPG、PNG等）
   - 图片会自动添加到模拟器的相册中

## 方法二：使用 ADB 命令推送照片

1. **打开命令行/终端**

2. **推送图片到模拟器**
   ```bash
   # Windows
   adb push "C:\path\to\your\image.jpg" /sdcard/Pictures/

   # Mac/Linux
   adb push "/path/to/your/image.jpg" /sdcard/Pictures/
   ```

3. **刷新媒体库**
   ```bash
   adb shell am broadcast -a android.intent.action.MEDIA_SCANNER_SCAN_FILE -d file:///sdcard/Pictures/image.jpg
   ```

## 方法三：拖放图片到模拟器

1. **直接拖放**
   - 从电脑文件管理器中选择图片
   - 直接拖放到 Android 模拟器窗口
   - 图片会自动保存到 `/sdcard/Download/` 目录
   - 在相册应用中可以看到

## 方法四：使用 Device File Explorer

1. **打开 Device File Explorer**
   - 在 Android Studio 底部找到 **"Device File Explorer"** 标签
   - 或通过 `View` > `Tool Windows` > `Device File Explorer`

2. **上传文件**
   - 导航到 `/sdcard/Pictures/` 或 `/sdcard/DCIM/Camera/`
   - 右键点击文件夹
   - 选择 **"Upload..."**
   - 选择电脑中的图片文件

3. **刷新相册**
   - 在模拟器中打开 **Photos** 或 **Gallery** 应用
   - 图片应该会自动显示

## 测试应用的图片上传功能

1. **运行应用**
   - 在 Android Studio 中点击 Run 按钮
   - 等待应用安装并启动

2. **导航到上传页面**
   - 在首页点击 **"上传原型"** 按钮
   - 或点击 **"创建新项目"** 卡片

3. **选择上传方式**
   - 点击 **"相册选择"** 卡片
   - 系统会打开图片选择器

4. **选择图片**
   - 在图片选择器中选择一张或多张图片
   - 点击确认/完成

5. **查看预览**
   - 选中的图片会显示在预览网格中
   - 可以点击图片删除
   - 可以点击 **"添加更多"** 继续添加

6. **开始识别**
   - 点击 **"开始 AI 识别"** 按钮
   - 等待 2 秒分析
   - 自动跳转到项目详情页面

## 常见问题

### 问题1：相册中没有照片
**解决方案：**
- 确保已使用上述方法添加照片到模拟器
- 打开模拟器的 Photos 或 Gallery 应用确认照片存在
- 重启模拟器

### 问题2：应用无法访问照片
**解决方案：**
- 检查 `AndroidManifest.xml` 中是否已添加权限：
  ```xml
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  ```
- 在应用中授予存储权限
- 重新安装应用

### 问题3：选择图片后没有反应
**解决方案：**
- 查看 Logcat 日志，搜索 "UploadScreen" 标签
- 检查是否有错误信息
- 确认图片格式是否支持（JPG、PNG、GIF、WebP）

### 问题4：图片预览显示不出来
**解决方案：**
- 确保 Coil 库已正确添加到 `build.gradle.kts`
- 检查网络权限（如果图片来自网络）
- 查看 Logcat 中的 Coil 相关日志

## 调试技巧

1. **查看日志**
   ```
   在 Android Studio 的 Logcat 中过滤 "UploadScreen" 标签
   ```

2. **Toast 提示**
   - 应用会显示 Toast 提示操作状态
   - "已选择 X 张图片"
   - "未选择图片"
   - "开始AI识别..."

3. **检查权限**
   ```bash
   adb shell pm list permissions -d -g
   ```

## 推荐的测试图片

建议使用以下类型的图片进行测试：
- 手绘原型草图
- 线框图
- UI 设计稿
- 纸质原型照片

图片要求：
- 格式：JPG、PNG、GIF、WebP
- 大小：建议不超过 10MB
- 分辨率：建议 1080p 以下
